@model IndexViewModel

@{
    ViewData["Title"] = "Aviaries";
}

<h1 class="page-title">Homes for everyone</h1>

<div style="text-align: end;">
    <input class="btn btn-success" style="margin: 10px;" type="button" value="New home equipped" onclick="location.href='@Url.Action("Create", "Aviaries")'" />
</div>

<table class="table">
    <thead>
        <tr>
            <th>
                №
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Aviaries.FirstOrDefault().Condition)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Aviaries.FirstOrDefault().MaxAnimals)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Aviaries.FirstOrDefault().Type)
            </th>
            <th style="text-align:end">Actions</th>
        </tr>
    </thead>
    <tbody>
        @{int i = 1;}
        @foreach (var item in Model.Aviaries)
        {
            <tr>
                <td>
                    @i @{i++;}
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Condition)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.MaxAnimals)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Type)
                </td>
                <td style="text-align:end">
                    <a asp-action="Edit" asp-route-id="@item.Id" class="btn btn-sm btn-circle btn-edit"><i class="far fa-edit"></i></a>
                    <a asp-action="Details" asp-route-id="@item.Id" class="btn btn-sm btn-circle btn-info"><i class="fas fa-info"></i></a>
                    <a asp-action="Delete" asp-route-id="@item.Id" class="btn btn-sm btn-circle btn-danger"><i class="far fa-trash-alt"></i></a>
                </td>
            </tr>
        }
    </tbody>
</table>

@if (Model.PageViewModel.HasPreviousPage)
{
    <a asp-action="Index"
       asp-route-page="@(Model.PageViewModel.PageNumber - 1)"
       class="btn btn-outline-dark">
        <i class="fas fa-angle-left"></i>
        Previous
    </a>
}
@if (Model.PageViewModel.HasNextPage)
{
    <a asp-action="Index"
       asp-route-page="@(Model.PageViewModel.PageNumber + 1)"
       class="btn btn-outline-dark">
        Next
        <i class="fas fa-angle-right"></i>
    </a>
}